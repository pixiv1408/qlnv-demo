import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 65,
    borderTopColor: '#868',
    borderTopWidth: 1,
    flexDirection: 'row',
  }
});

export default styles;